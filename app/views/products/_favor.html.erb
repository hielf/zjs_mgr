
<%= form_for(user_broker(current_user).brokerproductrels.build(:product_id => @product.id), :remote => true) do |f| %>
  <%= f.hidden_field :product_id %>
	<%= f.submit "放入收藏夹", :class => 'btn btn-primary' %>
	<%= link_to '返回', products_path, :class => 'btn' %>
<% end %>