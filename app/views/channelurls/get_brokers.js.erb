$('.broker_selection').empty();
$('.broker_selection').append( $('<option></option>'));
<% @brokers.each do |broker| %>
  $('.broker_selection').append($('<option value="<%= broker.id %>"><%= broker.broker_code %> | <%= broker.broker_name %></option>'));
<% end %>